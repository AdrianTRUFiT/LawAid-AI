import React from "react";

export default function CodeCoordinatorIntakeWorkspace() {
  return (
    <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
      <h2 className="text-lg font-bold text-legal-navy">Code Coordinator Intake Workspace</h2>
      <p className="mt-2 text-sm text-slate-500">
        Intake workspace placeholder. This surface is present so the admin view can compile while the full coordinator workflow is reconciled.
      </p>
    </div>
  );
}